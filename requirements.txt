# AoEInsights mgz-fast (``mgz`` on PyPI). Happyleaves parser is vendored as ``legacy/mgz_legacy``;
# pin the same libs happyleaves ``mgz`` 1.8.51 used with the vendored tree.

Pillow
construct==2.8.16
aocref>=2.0.35
tabulate>=0.9.0
AoE2ScenarioParser
mgz-fast
genie-scx-py @ git+https://github.com/UnluckyForSome/genie-scx-py.git