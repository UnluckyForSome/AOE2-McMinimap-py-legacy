# Runtime deps mirror pyproject.toml (for workflows that still use requirements.txt).
# Editable dev install: pip install -e .

Pillow
construct==2.8.16
aocref>=2.0.35
tabulate>=0.9.0
AoE2ScenarioParser
mgz-fast
genie-scx-py>=0.1.0
